{
  "name": "readhub-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "packageManager": "npm@11.16.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "start": "turbo run start",
    "test": "turbo run test",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@readhub/config": "*",
    "turbo": "^2.3.3",
    "vitest": "^4.1.10"
  }
}
